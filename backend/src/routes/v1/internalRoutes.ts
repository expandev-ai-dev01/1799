import { Router } from 'express';

const router = Router();

// ADD FEATURE-SPECIFIC INTERNAL (AUTHENTICATED) ROUTES HERE
// Example:
// import orderRoutes from './orderRoutes';
// router.use('/order', orderRoutes);

export default router;
